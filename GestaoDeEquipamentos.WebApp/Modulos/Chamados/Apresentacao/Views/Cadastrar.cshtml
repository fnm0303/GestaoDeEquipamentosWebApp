@model CadastrarChamadoViewModel
@{
    Layout = "_Layout";
    ViewBag.Titulo = "Cadastro de Chamado";
}


<div class="row justify-content-start">
    <form class="col-lg-6" asp-action="Cadastrar" method="post">
        <input type="hidden" asp-for="DataAbertura" />
        <div>
            <label class="form-label" asp-for="Titulo"></label>
            <input class="form-control" asp-for="Titulo" type="text">
            <span class="text-danger" asp-validation-for="Titulo"></span>
        </div>

        <div class="mt-4">
            <label class="form-label" asp-for="Descricao">Descrição do chamado</label>
            <input class="form-control" asp-for="Descricao" type="text">
            <span class="text-danger" asp-validation-for="Descricao"></span>
        </div>

        @*<div class="mt-4">
            <label class="form-label" asp-for="DataAbertura">Data de Abertura</label>
            <input class="form-control" asp-for="DataAbertura" type="datetime-local" readonly>
            <span class="text-danger" asp-validation-for="DataAbertura"></span>
        </div>*@

        <div class="mt-4">
            <label class="form-label" asp-for="EquipamentoId">Equipamento</label>
            <select class="form-select" asp-for="EquipamentoId">
                <option value="0">Selecione um equipamento</option>
                @foreach (SelecionarEquipamentoViewModel equipamento in Model.EquipamentosDisponiveis!)
                {
                    <option value="@equipamento.Id">@equipamento.Nome</option>
                }
            </select>
            <span class="text-danger" asp-validation-for="EquipamentoId"></span>
        </div>

        <div class="d-flex gap-3 mt-4">
            <button class="btn btn-primary" type="submit">
                <i class="bi bi-check"></i>
                Cadastrar
            </button>
            <a class="btn btn-outline-secondary" asp-action="Listar">
                <i class="bi bi-arrow-return-left"></i>
                Voltar para a lista
            </a>
        </div>
    </form>
</div>